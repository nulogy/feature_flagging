#
# Determine which gems to load based on the current filename.
#
if File.basename(__FILE__) == "Gemfile.next"
  ENV["RAILS_NEXT"] = "true"
end

source "https://rubygems.org"

# Specify your gem's dependencies in feature_flagging.gemspec
gemspec
